export function CommentList({ postId }: { postId: string }) {
    return (
        <div className="pt-4 border-t mt-4 mb-2">
            <h4 className="text-sm font-semibold mb-3">Comments</h4>
            <div className="text-sm text-muted-foreground italic text-center py-4 bg-muted/30 rounded-lg border border-dashed border-muted">
                Comments will be loaded here. Stub for future implementation.
            </div>
        </div>
    );
}
